# --- General ---
# Operating System Files
.DS_Store
Thumbs.db
ehthumbs.db

# IDE / Editor Files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.suo
*.user
*.sw? # Vim swap files

# Log files
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data / Temporary files
pids/
*.pid
*.seed
*.pid.lock
*~
*.tmp

# --- Node.js Specific (Applies to both backend and frontend) ---
# Dependency directories - CRITICAL
node_modules/

# Optional npm/yarn cache directories
.npm/
.yarn/cache
.yarn/unplugged
.yarn/install-state.gz
.pnp.*

# --- Backend (Node.js / Express) Specific ---
# Environment variables file - CRITICAL
backend/.env

# Optional: Test coverage reports
backend/coverage/

# Optional: Uploads or generated files if your backend creates them locally
# backend/uploads/

# --- Frontend (Vue / Vite) Specific ---
# Build output directory - CRITICAL
frontend/dist/
frontend/.output/ # Nuxt build output (if you ever switch)

# Environment variables files - CRITICAL
frontend/.env
frontend/.env.*
!frontend/.env.example # IMPORTANT: Allow example env files if you create them

# Ignore all local environment files generated by Vite/Vue CLI convention
frontend/.env.local
frontend/.env.*.local

# Optional: Test coverage reports
frontend/coverage/

# Vite cache directory
frontend/.vite/
.vite-inspect/ # Vite inspect tool files

# --- Docker Specific ---
# Ignore build context archives if created manually
*.tar.gz

# --- Secrets / Configuration (Root Level) ---
# Root environment variables file for Docker Compose - CRITICAL
.env
.env.local
.env.*.local

# --- Other ---
# Optional: Add other generated files or folders specific to your tools
# e.g., documentation builds, etc.
# docs/_build/